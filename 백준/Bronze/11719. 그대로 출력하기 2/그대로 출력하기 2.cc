#include <bits/stdc++.h>

 using namespace std;  


int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
	string str;
	
	while(!cin.eof())
	{
		getline(cin, str);
		cout << str << "\n";
	}
	return 0;
}
